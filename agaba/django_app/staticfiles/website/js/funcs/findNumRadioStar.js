export async function findNumRadioStar() {
    // const ratings = document.querySelectorAll('.catalog_section.main .catalog_item .catalog_rating');

    // // ищем элементы
    // ratings.forEach((rating, index) => {
    //     const inputRating = rating.querySelector('input.catalog_star[id="catalogStar_"]');
    //     const labelRating = rating.querySelector('label.catalog_label[for="catalogStar_"]');


    //     if (inputRating && labelRating) {
    //         const newId = `catalogStar_${index + 1}`;
    //         inputRating.id = newId;
    //         labelRating.htmlFor = newId;
    //     }
    // });
}
